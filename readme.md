## Test execution command with open report

`pytest -v --html=report.html --self-contained-html ; start report.html`

## Update playwright to latest version

`pip install pytest-playwright playwright -U`


